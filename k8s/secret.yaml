# WARNING: This is a template file with placeholder values
# DO NOT commit actual secrets to version control
# 
# For production use, consider:
# 1. Kubernetes External Secrets Operator
# 2. Sealed Secrets
# 3. Cloud provider secret managers (AWS Secrets Manager, GCP Secret Manager, Azure Key Vault)
# 4. HashiCorp Vault
#
# To create the secret securely:
# kubectl create secret generic notification-job-secret \
#   --from-literal=DB_PASSWORD='your-actual-password' \
#   --from-literal=TWILIO_ACCOUNT_SID='ACxxxxxxxxxxxxx' \
#   --from-literal=TWILIO_AUTH_TOKEN='your-token' \
#   --from-literal=TWILIO_PHONE_NUMBER='+15551234567'

apiVersion: v1
kind: Secret
metadata:
  name: notification-job-secret
  namespace: default
type: Opaque
stringData:
  # Database credentials
  DB_PASSWORD: "your-database-password"
  
  # Twilio credentials
  TWILIO_ACCOUNT_SID: "your-twilio-account-sid"
  TWILIO_AUTH_TOKEN: "your-twilio-auth-token"
  TWILIO_PHONE_NUMBER: "+1234567890"
