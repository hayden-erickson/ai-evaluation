apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-cron-config
  namespace: default
data:
  # Database configuration
  DB_HOST: "mysql-service"  # Update this to your MySQL service name
  DB_PORT: "3306"
  DB_USER: "habits_app"     # Update this to your database user
  DB_NAME: "habits"         # Update this to your database name
