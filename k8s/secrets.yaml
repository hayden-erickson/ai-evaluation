apiVersion: v1
kind: Secret
metadata:
  name: habits-api-secrets
  namespace: habits-app
type: Opaque
stringData:
  # Database credentials
  DB_USER: "habits_user"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  DB_NAME: "habits_db"
  
  # JWT secret - MUST be changed in production
  JWT_SECRET: "CHANGE_ME_TO_A_RANDOM_SECRET_KEY"
